# OAuth 2.0 Configuration Template
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# Application Settings
APP_NAME=My FastAPI App
APP_ENV=development  # development, staging, production
DEBUG=True

# Security Settings (REQUIRED)
SECRET_KEY=your-secret-key-generate-with-generate_secret_key.py
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Auth0 Configuration (if using Auth0)
AUTH0_DOMAIN=your-domain.auth0.com
AUTH0_API_AUDIENCE=your-api-identifier
AUTH0_CLIENT_ID=your-client-id
AUTH0_CLIENT_SECRET=your-client-secret
AUTH0_ALGORITHMS=RS256

# Google OAuth Configuration (if using Google)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# Microsoft Azure AD Configuration (if using Azure)
AZURE_CLIENT_ID=your-azure-client-id
AZURE_CLIENT_SECRET=your-azure-client-secret
AZURE_TENANT_ID=your-tenant-id

# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/dbname
# For async: postgresql+asyncpg://user:password@localhost:5432/dbname

# CORS Settings
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8501
# Production: https://your-frontend.com

# Session Configuration
SESSION_SECRET=generate-with-generate_secret_key.py
SESSION_EXPIRE_MINUTES=30

# Logging
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL

# Rate Limiting
RATE_LIMIT_ENABLED=True
RATE_LIMIT_PER_MINUTE=60

# Production HTTPS Settings (set to True in production)
HTTPS_ONLY=False
SECURE_COOKIES=False

# Streamlit Configuration (for .streamlit/secrets.toml)
# Copy these to your Streamlit secrets file:
# STREAMLIT_REDIRECT_URI=http://localhost:8501/oauth2callback
# STREAMLIT_COOKIE_SECRET=generate-with-generate_secret_key.py
